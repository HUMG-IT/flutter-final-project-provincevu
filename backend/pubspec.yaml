name: backend
description: REST API Backend cho ứng dụng quản lý chi tiêu
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  # Firebase Admin SDK cho Dart (tuỳ chọn nếu cần kết nối Firebase từ server)
  # firedart: ^0.9.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
